# Workstreams

This file breaks the roadmap into practical engineering tracks that can run in parallel, with current priority given to CTF workflows and shell ergonomics.

## 1. CTF Command Model (Mostly Complete)

Primary outcome: the CTF workflow is coherent, learnable, and fast under pressure.

### Completed (alpha4–alpha6)

- ~~Implement the shelve system as the primary challenge completion command.~~
- ~~Add `--cd` flag to `ctf add` and make `work` a hidden alias.~~
- ~~Make `solve` and `done` hidden aliases for `shelve`.~~
- ~~Introduce `.challenge.json` for per-challenge metadata (replaces `flag.txt`).~~
- ~~Implement interactive file triage with configurable blacklist and whitelist.~~

### Remaining

- Normalize naming, help text, and examples around the core six commands.

### Success measure

- A user can understand the intended CTF flow without guessing which command to use next.

## 2. Shell Integration And Completion (Mostly Complete)

Primary outcome: Wardex feels native in Bash and Zsh.

### Completed (alpha6 → 0.3.0-alpha3)

- ~~Design completion support for subcommands, event names, categories, challenge names, and likely flags.~~ (RFC 0002 accepted)
- ~~Decide whether completions are generated by Clap, maintained as scripts, or exposed through a dedicated command.~~ (`clap_complete` via `wardex completions`)
- ~~Add shell install instructions and a minimal setup path for Bash and Zsh.~~
- ~~Implement static completion for all commands and flags.~~
- ~~Dynamic completion for event names, categories, and challenge paths.~~ (alpha8, 0.3-alpha1)
- ~~Path-arg tilde handling — no zsh user-list fallback on bare `~<TAB>`.~~ (0.3-alpha3)
- ~~Instruction-based completers — no silent fallback to guessed contexts.~~ (0.3-alpha2)

### Remaining

- Review `ctf path --cd` and `ctf add --cd` docs and add a Bash/Zsh wrapper
  example to README. (Low-risk; code already escape-hardened in `3d2eb79`.)
- Shell-integration smoke test that actually invokes `compgen`/`compadd` —
  unit tests for the completer functions exist; an end-to-end shell pass
  would close T018.

### Success measure

- Tab completion and shell navigation meaningfully reduce typing and context switching. **Met.**

## 3. Quality And Safety

Primary outcome: competition-critical commands are trustworthy.

### Completed (through 0.3.0-alpha3)

- ~~Resolve `assert_cmd::Command::cargo_bin` deprecation warnings (14 sites).~~ (alpha3, `1ffcaa1`)
- ~~Resolve clippy `unnecessary_sort_by` + toolchain-drift causing 4 red CI runs.~~ (alpha3, `75aacae`)
- ~~Regression tests for `archive` error path, partial-name matching, `finish --no-archive`.~~ (alpha3, `26efc36`)

### Near-term tasks (0.4.x)

- Add targeted integration tests for remaining single-test lifecycle commands
  (`schedule`, `check`, `recent`, `finish` end-time metadata + error paths).
  T017 phase 2.
- Review guardrails for destructive operations (`finish --force`,
  `archive` on active event).
- Add regression tests around `ContextResolver` once T012 lands (that refactor
  supplies a unit-testable seam that doesn't exist today).
- Pin CI and devshell rust toolchains to the same version — document in
  README. T021.

### Success measure

- High-risk CTF commands have ≥3 integration tests each and clear failure
  behavior. (Currently 13 of 16 CTF commands meet this; 3 — `schedule`,
  `check`, `recent` — are the remaining gap.)

## 4. Templates And Writeups

Primary outcome: category-specific workflows feel intentional instead of generic.

### Near-term tasks

- Improve challenge scaffolding and category-specific templates.
- Define a default notes structure that feeds writeup generation cleanly.
- Review how `shelve` and `writeup` interact with notes, `.challenge.json`, and archives.
- Add example workflows for solo events and quick challenge pivots.

### Success measure

- The default challenge folder feels useful immediately after creation or import.

## 5. Distribution And Developer Operations

Primary outcome: releases and shell features are easy to install and verify.

### Completed (through 0.3.0-alpha3)

- ~~Add a simple CI workflow with formatting, linting, and tests.~~ (`.github/workflows/ci.yml`, green as of `75aacae`)
- ~~Migrate Nix build to `crane` for incremental dep caching.~~ (0.3-alpha3, `06e6955`)
- ~~Resolve Home Manager `programs.zsh.initExtra` deprecation.~~ (alpha2)

### Near-term tasks (0.4.x)

- Pin CI and devshell rust toolchains to the same version — see T021.
- Add completion verification smoke test — see T018.
- Validate Nix and Cargo installation paths against shell integration docs
  (README lists `cargo install` and `nix run`; the HM module workflow needs a
  short validation pass).
- Document platform assumptions for Bash and Zsh users in the README shell
  integration section.

### Success measure

- Releases are repeatable and documented without relying on tribal knowledge. **Met for Nix path; partial for Cargo path.**

## Suggested Next Backlog (0.3.0-alpha4 → beta1 → 0.3.0)

0.3.x feature work is implemented across alpha1–alpha3; the remaining work
to reach stable **0.3.0** is test depth + hygiene, not new features.

1. **T017 phase 2** — close remaining single-test lifecycle commands
   (`schedule`, `check`, `recent`, `finish` error paths). Required for
   beta1 cut.
2. **T021** toolchain pin — one commit, prevents the fmt/clippy drift class.
   Required for beta1 cut.
3. **T013** `ctf path --cd` / `add --cd` README wrappers — low-risk docs
   pass; ships with alpha4 or beta1.
4. **T018** completion smoke test — invoke `compgen`/`compadd` from a Bash
   test to verify end-to-end. Optional for beta1.

Deferred to 0.4.x (post-0.3.0-stable):

- **T012** `ContextResolver` refactor — whole-binary refactor, wrong shape
  for a stabilization beta. Opens 0.4.x.
